# =============================================================================
# DEPENDÊNCIAS PYTHON PARA SISTEMA DE CÂMERAS HOMEGUARD
# =============================================================================
# Use: pip install -r requirements_camera.txt
# =============================================================================

# === PROCESSAMENTO DE VÍDEO E VISÃO COMPUTACIONAL ===
opencv-python==4.8.1.78          # Processamento de vídeo e detecção de movimento
numpy==1.24.3                     # Operações matemáticas para processamento de imagem

# === COMUNICAÇÃO E REDE ===
paho-mqtt==1.6.1                  # Cliente MQTT para integração com HomeGuard
requests==2.31.0                  # Requisições HTTP para API das câmeras Intelbras
urllib3==2.0.4                    # Biblioteca HTTP de baixo nível

# === INTERFACE WEB ===
Flask==2.3.3                      # Framework web para interface de controle
Werkzeug==2.3.7                   # Utilitários WSGI para Flask
Jinja2==3.1.2                     # Template engine para Flask
MarkupSafe==2.1.3                 # Escape HTML seguro para Jinja2

# === BANCO DE DADOS ===
# SQLite já vem com Python, sem dependências extras necessárias

# === PROCESSAMENTO DE DATA/HORA ===
pytz==2023.3                      # Fusos horários (se necessário)

# === UTILITÁRIOS ===
python-dotenv==1.0.0              # Carregar variáveis de ambiente (opcional)
schedule==1.2.0                   # Agendamento de tarefas (opcional)

# === MONITORAMENTO E LOGS ===
psutil==5.9.5                     # Monitoramento de sistema (CPU, memória)

# === CODIFICAÇÃO DE VÍDEO (OPCIONAL) ===
# Descomente se precisar de codificação avançada de vídeo
# ffmpeg-python==0.2.0            # Interface Python para FFmpeg

# === NOTIFICAÇÕES (OPCIONAL) ===
# Descomente se quiser notificações por email
# smtplib já vem com Python
# email-validator==2.0.0          # Validação de emails

# === DESENVOLVIMENTO E DEBUG (OPCIONAL) ===
# Descomente para ambiente de desenvolvimento
# ipython==8.14.0                 # REPL interativo melhorado
# pytest==7.4.0                   # Framework de testes

# =============================================================================
# NOTAS DE INSTALAÇÃO:
# =============================================================================
#
# 1. RASPBERRY PI:
#    Algumas dependências podem precisar de compilação. Para acelerar:
#    sudo apt update
#    sudo apt install python3-opencv python3-numpy -y
#    Depois: pip install -r requirements_camera.txt --no-deps opencv-python numpy
#
# 2. OPENCV NO RASPBERRY PI:
#    Se houver problemas com OpenCV, instale via apt:
#    sudo apt install python3-opencv
#    E remova opencv-python desta lista
#
# 3. MEMÓRIA INSUFICIENTE:
#    Se falhar por memória, instale um por vez:
#    pip install opencv-python
#    pip install paho-mqtt
#    pip install flask
#    etc...
#
# 4. VERSÕES ESPECÍFICAS:
#    As versões são fixadas para garantir compatibilidade.
#    Para atualizar: pip install --upgrade package_name
#
# =============================================================================
